#include <stdio.h>
/**
 * main -print alphabet in lowercase followed by a new line
 *
 * Return: 0
 */

int mai(void)
{
	char f;

	f = 'a';
	while
		(f <= 'z') {
			if ((f != 'q' && f != 'e') && f <= 'z')
			putchar(f);
			f++;
		}
	putchar('\n');
	return (0);
}
