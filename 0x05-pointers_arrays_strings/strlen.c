#include "main.h"
#include <string.h>

/**
 * _strlen - function to get the length of string
 *
 * @s - String to pass this function
 *
 * Return: returns the length of a string.
 */
int _strlen(char *s)
{
	return (strlen(s));
}
