#include "main.h"
#include <string.h>

/**
 * Main - function that returns the length of a string
 *
 * @s: to pass the function
 *
 * Return: length of a string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
